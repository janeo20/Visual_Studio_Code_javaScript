let Base, Altura, Area;

Base = prompt ("Digite o valor da base: ")

Altura = prompt ("Digite o valor da altura: ")

if (Base > 0 && Altura > 0){
   Area = (Base * Altura) / 2
   alert ("A Area desse triângulo é: " + Area)
}else{}